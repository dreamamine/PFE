{% extends "LGMAdministrationBundle::layout.html.twig" %}
{% block page_body %}
    <h3> Voulez vous vraiment supprimer le code : <strong style="color:red;">
            {{groupe.nom}}</strong> de votre groupe </h3>
<form method="POST" action="{{path('delete_groupe', {'token': groupe.token} )}}">
 <input type="submit" class="btn btn-danger" value="Oui" />   
<a href="{{path("index_groupe")}}" class="btn btn-warning">Non</a>
{{form_rest(form)}}
</form>
        {% endblock %}