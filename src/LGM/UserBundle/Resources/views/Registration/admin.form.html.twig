{% extends "LGMUserBundle::layout.html.twig" %}

{% block body %}
<   {% trans_default_domain 'FOSUserBundle' %}

    <form action="{{ path('fos_admin_registration') }}" {{ form_enctype(form) }} method="POST">
        {{ form_widget(form) }}
        <div>
            <input type="submit" value="{{ 'registration.submit'|trans }}" />
        </div>
    </form>
   
{% endblock body %}